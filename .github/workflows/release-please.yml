name: Release Please

on:
  push:
    branches:
      - master


jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: GoogleCloudPlatform/release-please-action@v2
        with:
          release-type: simple
